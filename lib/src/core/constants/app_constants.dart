class HiveBoxes {
  const HiveBoxes._();

  static const String settings = 'settings_box';
  static const String workspace = 'workspace_box';
}

class HiveKeys {
  const HiveKeys._();

  static const String config = 'workspace_config';
}
